<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.hiepdt.tinderapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".login.ui.login.EmailLoginActivity"
            android:label="@string/title_activity_email_login"></activity>
        <activity android:name=".profile.SettingActivity" />
        <activity android:name=".profile.UploadActivity" />
        <activity android:name=".profile.EditActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".login.PhoneLoginActivity" />
        <activity android:name=".login.PhoneVerifyActivity" />
        <activity android:name=".login.info.InfoActivity" />
        <activity android:name=".messenger.InboxActivity" />
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>